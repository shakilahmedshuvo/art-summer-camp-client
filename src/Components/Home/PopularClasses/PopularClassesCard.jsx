const PopularClassesCard = ({ singleClasses }) => {
    const { id, name, studentNumber, info } = singleClasses;
    console.log(singleClasses);
    return (
        <div>

        </div>
    );
};

export default PopularClassesCard;